<header class="bg-white shadow-md fixed top-0 left-0 right-0 z-50">
  <nav class="max-w-7xl mx-auto px-4 py-3 flex items-center justify-between">
    <!-- Logo -->
    <a href="/" class="flex items-center">
      <img
        src="https://res.cloudinary.com/dvmtn1i7u/image/upload/v1759337257/logo_kihjes.png"
        alt="Estarlie Logo"
        class="h-10 max-w-[160px] w-auto"
        loading="lazy"
      />
    </a>

    <!-- Desktop Menu -->
    <ul class="hidden md:flex space-x-8 font-medium text-[#203844]">
      <li><a href="/" class="hover:text-[#FDB414]">Home</a></li>
      <li><a href="/about" class="hover:text-[#FDB414]">About</a></li>
      <li><a href="/services" class="hover:text-[#FDB414]">Services</a></li>
      <li><a href="/contact" class="hover:text-[#FDB414]">Contact</a></li>
    </ul>

    <!-- Mobile Hamburger -->
    <button
      id="menu-btn"
      class="md:hidden text-[#203844] h-8 w-8 focus:outline-none"
      aria-label="Toggle menu"
    >
      <!-- Hamburger Icon -->
      <svg
        id="icon-hamburger"
        xmlns="http://www.w3.org/2000/svg"
        class="h-8 w-8 block"
        fill="none"
        viewBox="0 0 24 24"
        stroke="currentColor"
        aria-hidden="true"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M4 6h16M4 12h16M4 18h16"
        />
      </svg>
      <!-- Close Icon -->
      <svg
        id="icon-close"
        xmlns="http://www.w3.org/2000/svg"
        class="h-8 w-8 hidden"
        fill="none"
        viewBox="0 0 24 24"
        stroke="currentColor"
        aria-hidden="true"
      >
        <path
          stroke-linecap="round"
          stroke-linejoin="round"
          stroke-width="2"
          d="M6 18L18 6M6 6l12 12"
        />
      </svg>
    </button>
  </nav>

  <!-- Mobile Dropdown (No overlay, positioned below header) -->
  <div
    id="mobile-menu"
    class="absolute top-full left-0 right-0 max-h-0 overflow-hidden transition-all duration-300 ease-in-out md:hidden bg-white shadow-lg z-40"
  >
    <ul class="flex flex-col space-y-4 p-4 text-[#203844] font-medium">
      <li class="opacity-0 -translate-y-2 transition-all duration-300 delay-100">
        <a href="/" class="hover:text-[#FDB414]">Home</a>
      </li>
      <li class="opacity-0 -translate-y-2 transition-all duration-300 delay-200">
        <a href="/about" class="hover:text-[#FDB414]">About</a>
      </li>
      <li class="opacity-0 -translate-y-2 transition-all duration-300 delay-300">
        <a href="/services" class="hover:text-[#FDB414]">Services</a>
      </li>
      <li class="opacity-0 -translate-y-2 transition-all duration-300 delay-500">
        <a href="/contact" class="hover:text-[#FDB414]">Contact</a>
      </li>
    </ul>
  </div>

  <!-- Script -->
  <script>
  document.addEventListener('DOMContentLoaded', () => {
    const btn = document.getElementById('menu-btn');
    const menu = document.getElementById('mobile-menu');
    const iconHamburger = document.getElementById('icon-hamburger');
    const iconClose = document.getElementById('icon-close');

    // ✅ Exit if any element is missing (safe and quiet)
    if (!btn || !menu || !iconHamburger || !iconClose) return;

    const links = menu.querySelectorAll('li');

    const toggleMenu = () => {
      const isOpen = menu.classList.contains('max-h-[500px]');

      if (isOpen) {
        menu.classList.remove('max-h-[500px]');
        menu.classList.add('max-h-0');
        iconHamburger.classList.remove('hidden');
        iconClose.classList.add('hidden');

        links.forEach(link => {
          link.classList.add('opacity-0', '-translate-y-2');
        });
      } else {
        menu.classList.remove('max-h-0');
        menu.classList.add('max-h-[500px]');
        iconHamburger.classList.add('hidden');
        iconClose.classList.remove('hidden');

        links.forEach(link => {
          setTimeout(() => {
            link.classList.remove('opacity-0', '-translate-y-2');
          }, 100);
        });
      }
    };

    btn.addEventListener('click', toggleMenu);
  });
</script>

</header>
